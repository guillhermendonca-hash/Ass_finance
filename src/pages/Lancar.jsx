import EmBreve from '../components/EmBreve'

export default function Lancar() {
  return (
    <EmBreve
      titulo="Lançar"
      subtitulo="Próxima etapa"
      descricao="Gasto ou recebimento em 2–3 toques: valor, classe, categoria, vínculo e visibilidade."
    />
  )
}
